# 1.8.1：限時人工保護與全圖最近觀測

人工輸入計時、階段、ON 或擊殺後，卡片暫停接受自動代寫，預設 5 分鐘；偵測器抽屜的設定可選 3、4、5 分鐘。每次人工填寫重新計時，卡片顯示剩餘保護時間。到期自動恢復代寫資格，不需按「恢復自動」；只接受晚於到期時間的新確認觀測，保護期間的舊結果不能回填。觀測簿仍獨立收集資料。

保護期限隨人工卡片共享，同房 1.8.1 頁面遵守相同期限；重新整理與偵測心跳不會延長。修改本機設定只影響之後的人工填寫。1.8.0 的持續人工鎖、無來源標記的舊卡及舊客戶端留下不符的 metadata，改由卡片原始修改時間起算 5 分鐘，已過期即可等待新觀測接手。時間欄位異常時顯示請重新填寫，避免無依據覆寫。

## 更新與使用

1. 發布網頁及四個 JS 檔案，確認 Pages 內容已更新後，由使用者把根節點 `app_version` 更新為字串 `1.8.1`。
2. 所有負責代寫的網頁都要更新。1.8.0 仍會持續保護，更早版本不檢查新保護欄位；前端保護無法約束仍在運作的舊版。
3. 偵測器需要新版 `poc_detect.py` 與 `observation.py`。本輪未重打包 exe；舊 exe 無法提供新契約的確認觀測。
4. 到期不會直接改卡片；仍需要「自動代寫」開啟、偵測器認領與有效的新確認。階段、倒數、ON 確認冷卻轉 KILLED 都遵守期限。仍保留同一大階段內的小數精度保護，例如 R1.5 不倒退成 R1。
5. 「全圖觀測」顯示最近／過期／來源分歧／從未觀測，可搜尋地圖及調整 1、2、5、10、30 分鐘過期門檻，預設 5 分鐘。

## 資料契約

- `observation-core.js`：純函式、人工卡片版本指紋、資料檢核、名稱與等級解析、來源分歧判斷。
- `map-catalog.js`：本地字典候選名冊，106 筆啟用與 3 筆停用。Lv.70 的兩張地圖有獨立識別，`70男`／`70水` 只作現有卡片別名。未查核分流不猜 CH.1。
- `observation-ui.js`：房間隔離的觀測保存及總覽，不寫卡片。不在名冊或身份不明的資料不補進名冊。
- Python 的 `observation` 是 schema 1：地圖等級、精確名稱、CH、階段、毫秒觀測時間、`same_frame_map_channel_stage` 證據類型。只接受當幀身份與當幀階段證據；冷卻記為 WAITING，不能直接當作 KILLED。遮擋或心跳沿用紀錄時保持原時間，轉場／位置作廢時清除。新確認紀錄可每 10 秒上傳；未改辨識門檻、ROI 或模板。
- 保存路徑：`rooms/<room>/observations/<catalog_id>_<ch>/<encoded_collector_id>`。每個目標與來源只留最近有效觀測，包含偵測器接收時間及保存端伺服器時間；同時保留不同來源以顯示分歧，舊資料不能倒蓋新資料。
- 遊戲伺服器尚未建立跨房間映射，資料不跨房合併。同房請使用同一遊戲伺服器。候選名冊與卡片／曾觀測分流都不是已驗證的全遊戲完整分母。
- 觀測保存由認領者執行，另勾代寫他人時可保存同房來源；預設開啟，可在總覽關閉。與「卡片自動代寫」開關獨立。
- 人工卡片欄位：`manualOverride` schema 2 保存 `minutes`、`at`、`until`、`signature`，期限以匹配指紋的卡片 `startTime + minutes` 計算，與本機設定無關。`active:true` 代表人工來源，是否仍在保護期由期限判斷；到期不需寫入解鎖紀錄。自動接手後 `active:false`，`autoGuard.notBefore` 保留原期限，阻擋舊觀測重送。所有自動卡片交易重新檢查保護狀態及讀取時版本；失敗不記成成功、不提前去重。
- 沒有修改 Firebase rules。規則拒絕或斷線時，總覽顯示錯誤；不將未載入冒充從未觀測，也不宣稱已保存。

## 驗證

`node --test qa_cooldown.test.cjs qa_coverage.test.cjs qa_observation.test.cjs`

測試在阻擋對外網路的 Chrome 與記憶體 Firebase 中執行。涵蓋 3／4／5 分鐘邊界、人工修改重新計時、跨頁面期限一致、舊鎖到期、心跳不延長、倒數顯示、到期三條代寫路徑、同冷卻週期重新填 ON、交易競爭、小數精度與既有觀測／覆蓋回歸。1.8.1 未修改 Python；沿用 1.8.0 偵測器。
